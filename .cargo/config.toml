# roci cargo configuration.
#
# The `ci` build profile is defined in the workspace Cargo.toml (cargo does not
# read profiles from this file). This file carries registry, network, and
# terminal settings shared by every cargo invocation.

[registries.crates-io]
protocol = "sparse"

[net]
retry = 3

[term]
color = "always"
